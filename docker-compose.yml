version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER: ism_user
      POSTGRES_PASSWORD: ism_password
      POSTGRES_DB: ismfinance
    ports:
      - "${DB_PORT:-5434}:5432" # Exposto para acesso externo se necessário (DBeaver, psql, etc)
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - ism_network

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    restart: always
    depends_on:
      - postgres
    environment:
      # Sobrescreve o DATABASE_URL para apontar para o contêiner interno do postgres
      - DATABASE_URL=postgresql://ism_user:ism_password@postgres:5432/ismfinance
      - PORT=5000
      - NODE_ENV=production
      - RUN_LOCAL=true
    env_file:
      - .env # Carrega os demais segredos como SMTP e JWT_SECRET
    ports:
      - "${API_PORT:-5007}:5000" # Exposto localmente, útil para debug direto
    networks:
      - ism_network

  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    restart: always
    depends_on:
      - api
    ports:
      - "${WEB_PORT:-85}:80" # Mapeia a porta dinâmica do host para a porta 80 do Nginx
    networks:
      - ism_network

volumes:
  pgdata:
    driver: local

networks:
  ism_network:
    driver: bridge
